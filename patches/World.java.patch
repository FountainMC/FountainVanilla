--- minecraft/src/net/minecraft/world/World.java	2016-06-04 22:55:04.477727141 -0700
+++ src/main/java/net/minecraft/world/World.java	2016-06-05 00:14:38.175241799 -0700
@@ -144,11 +144,6 @@
                public String call() throws Exception {
                   return CrashReportCategory.getCoordinateInfo(var1);
                }
-
-               // $FF: synthetic method
-               public Object call() throws Exception {
-                  return this.call();
-               }
             });
             throw new ReportedException(var4);
          }
@@ -407,11 +402,6 @@
                      return "ID #" + Block.getIdFromBlock(var2);
                   }
                }
-
-               // $FF: synthetic method
-               public Object call() throws Exception {
-                  return this.call();
-               }
             });
             CrashReportCategory.addBlockInfo(var6, var1, var3);
             throw new ReportedException(var5);
@@ -1994,11 +1984,11 @@
    }
 
    public <T extends Entity> List<T> getEntities(Class<? extends T> var1, Predicate<? super T> var2) {
-      ArrayList var3 = Lists.newArrayList();
+      ArrayList<T> var3 = Lists.newArrayList();
 
       for(Entity var5 : this.loadedEntityList) {
-         if(var1.isAssignableFrom(var5.getClass()) && var2.apply(var5)) {
-            var3.add(var5);
+         if(var1.isAssignableFrom(var5.getClass()) && var2.apply((T) var5)) {
+            var3.add((T) var5);
          }
       }
 
@@ -2006,11 +1996,11 @@
    }
 
    public <T extends Entity> List<T> getPlayers(Class<? extends T> var1, Predicate<? super T> var2) {
-      ArrayList var3 = Lists.newArrayList();
+      ArrayList<T> var3 = Lists.newArrayList();
 
       for(Entity var5 : this.playerEntities) {
-         if(var1.isAssignableFrom(var5.getClass()) && var2.apply(var5)) {
-            var3.add(var5);
+         if(var1.isAssignableFrom(var5.getClass()) && var2.apply((T) var5)) {
+            var3.add((T) var5);
          }
       }
 
@@ -2457,21 +2447,11 @@
          public String call() {
             return World.this.playerEntities.size() + " total; " + World.this.playerEntities.toString();
          }
-
-         // $FF: synthetic method
-         public Object call() throws Exception {
-            return this.call();
-         }
       });
       var2.setDetail("Chunk stats", new ICrashReportDetail<String>() {
          public String call() {
             return World.this.chunkProvider.makeString();
          }
-
-         // $FF: synthetic method
-         public Object call() throws Exception {
-            return this.call();
-         }
       });
 
       try {
